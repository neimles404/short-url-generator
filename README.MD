# 🧩 URL Shortener (Java, консольное приложение)

Сервис сокращения ссылок с поддержкой лимита переходов, сроком действия (TTL), пользовательскими настройками и файловой JSON-базой данных.
Реализован в соответствии с принципами **SOLID**, **KISS** и **DRY**, покрыт модульными и интеграционными тестами (покрытие > 50%).

---

## 🚀 Возможности

- Создание коротких ссылок (уникальных для каждого пользователя).
- Лимит переходов и автоматическая блокировка после его исчерпания.
- Ограничение времени жизни ссылки (TTL).
- Мультипользовательский режим с идентификацией по UUID.
- Автоматическая очистка просроченных ссылок в отдельном потоке.
- Хранение данных в файлах `links.json` и `users.json`.
- Консольное управление (без web-сервера).
- Покрытие тестами и автоматическая сборка через GitHub Actions.

---

## ⚙️ Требования

- Java **17+**
- Maven **3.8+**
- Git (для клонирования и CI)
- Аккаунт GitHub

---

## 🛠 Установка и запуск

### 1. Клонировать репозиторий

```bash
git clone https://github.com/your-account/url-shortener.git
cd url-shortener
````

### 2. Сборка и тесты

```bash
mvn clean verify
```

Эта команда:

* компилирует проект,
* запускает все тесты,
* проверяет стиль кода (Spotless),
* формирует отчёт покрытия JaCoCo (`target/site/jacoco/index.html`).

### 3. Запуск приложения

#### Через IntelliJ IDEA

Открой проект → класс `app.ConsoleApplication` → **Run**.

#### Через терминал

```bash
mvn exec:java -Dexec.mainClass="app.ConsoleApplication"
```

или

```bash
java -cp target/url-shortener-1.0-SNAPSHOT.jar app.ConsoleApplication
```

---

## ⚙️ Конфигурация

Файл: **`src/main/resources/app.properties`**

```properties
app.ttl.hours=24
app.shortCode.length=6
app.clicks.min=1
app.clicks.max=1000
app.clicks.default=10
app.db.file=links.json
app.users.db.file=users.json
app.base.url=clck.ru
```

Файлы базы:

* `links.json` — хранилище сокращённых ссылок.
* `users.json` — хранилище профилей пользователей и их настроек.

Создаются автоматически при первом запуске.

---

## 💻 Использование

### Первый экран

```text
=== Сервис сокращения ссылок ===
1) Ввести существующий UUID пользователя
2) Создать нового пользователя
0) Выход из приложения
```

* **1** — ввести UUID (найти существующего пользователя).
* **2** — создать нового пользователя, получить UUID.
* **0** — выйти.

UUID нужно сохранить — он используется для входа в свой профиль.

---

### Главное меню

```text
Меню:
1) Создать короткую ссылку
2) Перейти по короткой ссылке
3) Показать мои ссылки
4) Удалить мою ссылку
5) Изменить мои настройки (лимит и TTL)
0) Выход
```

#### Пример: создание короткой ссылки

```
Введите длинный URL: https://www.baeldung.com/java-9-http-client
```

Вывод:

```
Короткая ссылка создана!
Код: Ab3XyZ
Полная короткая ссылка: clck.ru/Ab3XyZ
Лимит переходов: 10
Истекает: 2026-01-14T12:34:56Z
```

#### Пример: переход по короткой ссылке

```
Введите короткий код: Ab3XyZ
```

Откроется оригинальный URL в браузере.
Если срок жизни истёк или лимит переходов исчерпан — появится уведомление:

```
⚠️ Ссылка недоступна: срок жизни истёк.
⚠️ Ссылка недоступна: лимит переходов исчерпан.
❌ Ссылка не найдена.
```

#### Пример: изменение настроек пользователя

```
Введите новый лимит переходов (или пусто): 20
Введите новый TTL ссылок (часов): 48
✅ Настройки обновлены.
```

---

## 🧱 Архитектура проекта

```
src/
├── db/                         # база данных (*.json)
├── main/
│   ├── java/
│   │   ├── app/                # консольный интерфейс (ConsoleApplication)
│   │   ├── config/             # конфигурация (AppConfig)
│   │   ├── model/              # модели (ShortLink, UserProfile)
│   │   ├── repository/         # репозитории (FileJson*)
│   │   ├── service/            # бизнес-логика и фоновые сервисы
│   │   └── exception/          # собственные исключения
│   └── resources/
│       └── app.properties
└── test/
    └── java/                   # unit и интеграционные тесты
```

### Основные классы


| Компонент              | Описание                                                                               |
| ------------------------------- | ---------------------------------------------------------------------------------------------- |
| **AppConfig**                   | Загружает TTL, лимиты и пути к JSON-файлам.                         |
| **ShortLink**                   | Модель сокращённой ссылки (id, shortCode, URL, лимит, TTL).        |
| **UserProfile**                 | Модель пользователя с UUID, лимитом и TTL.                          |
| **FileJsonShortLinkRepository** | Чтение/запись ссылок из`links.json`.                                       |
| **FileJsonUserRepository**      | Чтение/запись пользователей из`users.json`.                         |
| **UrlShortenerService**         | Бизнес-логика (создание, открытие, удаление ссылок). |
| **UserService**                 | Управление пользователями и их настройками.              |
| **ExpirationCleanupService**    | Фоновая очистка истекших ссылок (каждый час).             |
| **ConsoleApplication**          | Текстовый интерфейс (меню и команды).                            |

---

## 🧩 Ключевые решения

* **SOLID:** каждая сущность отвечает за одну задачу.
  Сервисы зависят от интерфейсов репозиториев (DIP).
* **KISS:** простая структура (без Spring, без БД — JSON-файлы).
* **DRY:** общие методы вынесены, повторяющийся код отсутствует.
* **Устойчивость:** фоновая очистка (`ExpirationCleanupService`) + проверка TTL при каждом действии.
* **Уникальность:** короткий код генерируется случайно (`SecureRandom`), уникальность гарантируется репозиторием.
* **Мультипользовательский режим:** каждый пользователь имеет UUID, свои лимиты и TTL.

---

## 🧪 Тестирование

### Запуск

```bash
mvn test
# или
mvn verify
```

### Отчёт покрытия

```
target/site/jacoco/index.html
```

Открывается в браузере.
Среднее покрытие по проекту — **>50%**.

### Покрытые сценарии

#### Unit-тесты

* `UrlShortenerService`

  * генерация уникальных ссылок;
  * блокировка по лимиту переходов;
  * удаление по истечении TTL;
  * недоступность чужих ссылок;
  * валидация URL.
* `UserService`

  * создание/редактирование профиля пользователя;
  * обработка несуществующих пользователей.
* `ShortLink`

  * проверка `isExpired()` и `isClickLimitExceeded()`.

#### Интеграционные тесты

* `FileJsonShortLinkRepository` и `FileJsonUserRepository`

  * чтение/запись JSON;
  * поиск, удаление, очистка `deleteExpired`.
* `ConsoleApplication`

  * сценарии запуска и выхода, создание пользователя.
* `ExpirationCleanupService`

  * периодический вызов очистки.
* `AppConfig`

  * валидация параметров конфигурации.

---

## 🔄 CI / CD (GitHub Actions)

Файл: **`.github/workflows/ci.yml`**

```yaml
name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build-test-lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout sources
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: microsoft
          java-version: 17
          cache: maven

      - name: Build, test, lint (Spotless) and coverage
        run: mvn -B verify

      - name: Archive JaCoCo report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: jacoco-report
          path: target/site/jacoco
```

---

## 🧹 Линтинг и стиль кода

Форматирование кода обеспечивается плагином **Spotless** + **Google Java Format**.
Проверка запускается автоматически при `mvn verify`.

* Исправить форматирование:

  ```bash
  mvn spotless:apply
  ```
* Проверить стиль:

  ```bash
  mvn spotless:check
  ```

Конфигурация хранится в `pom.xml`, а общие правила IDE — в `.editorconfig`.
